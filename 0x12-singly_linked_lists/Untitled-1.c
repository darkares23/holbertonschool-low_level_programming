typedef struct list_s
{
	char *str;
	unsigned int len;
	struct list_s *next;
} list_t;

count = 0;

while (link_list->h)
{
	if (num_num == func_node)
	{
		while (link_list)
		{
			h = h->next;
			count++;
			if (h == node_dest)
			{
				return (count);
			}
		}
	}
	h = h-next;
}
